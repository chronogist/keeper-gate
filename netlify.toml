[build]
  base    = "docs"
  command = "npm install --legacy-peer-deps && npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION              = "22"
  npm_config_ignore_scripts = "true"
