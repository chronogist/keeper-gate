{
  "name": "elizaos-agent",
  "version": "0.0.1",
  "private": true,
  "description": "Pure ElizaOS agent that loads the @keepergate/elizaos plugin via the official CLI.",
  "scripts": {
    "start": "elizaos start --character ./character.json",
    "dev": "elizaos start --character ./character.json"
  },
  "dependencies": {
    "@elizaos/cli": "^1.7.2",
    "@elizaos/plugin-bootstrap": "^1.7.2",
    "@elizaos/plugin-openai": "^1.6.0",
    "@elizaos/plugin-sql": "^1.7.2",
    "@keepergate/elizaos": "workspace:*"
  }
}
